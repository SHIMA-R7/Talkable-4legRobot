# requirements-edge.txt
# Raspberry Pi 3B (64bit) / Python 3.13
#
# 事前にOSパッケージが必要:
#   sudo apt install -y python3-pip python3-venv python3-dev \
#       portaudio19-dev libopenblas-dev i2c-tools espeak-ng git
#
# I2C/SPI を raspi-config で有効化し、overlayroot は無効化しておくこと
#   sudo raspi-config nonint do_overlayfs 0

# 通信
websockets
pydantic
pyyaml
python-dotenv

# 音声
pyaudio
vosk
numpy

# ハードウェア制御
# サーボ: 旧Adafruit_PCA9685ライブラリ (busnum=1 明示指定が必要なため)
Adafruit_PCA9685
RPi.GPIO

# ディスプレイ (SPI LCD)
luma.lcd
spidev
pillow

# ユーティリティ
psutil
